//Creates a new instance of Ractive associated with a DOM container, using a specific template and initial data.
var ractive = new Ractive({
  el: '#container',
  template: '#template',
  data: {}
});